# MeatBiz V65.6 - Compact AI Blocks

## Frontend
- POS: `AI nhập hàng / tồn kho` mặc định thu gọn, chỉ mở khi cần dùng.
- POS: `AI Voice POS` giữ thu gọn mặc định và giảm chiều cao khi thu gọn.
- Header `AI Ready` trên tất cả form được giảm chiều cao, giảm padding và font-size để tiết kiệm không gian.
- Giữ nguyên dialog sửa bill V65.5.

## Backend
- Không thay đổi, giữ API V65.2.

## Build
- `npm run build` OK.
